# 选择题自由练习101-150

---

## 题目区

101. 1.（全国联考2011下）以下关于图的叙述
     Ⅰ. 回路是简单路径
     Ⅱ. 存储稀疏图，用邻接矩阵比邻接表更省空间
     Ⅲ. 若有向图中存在拓扑序列，则该图不存在回路
     其中正确的是________。
     A. 仅Ⅰ
     B. 仅Ⅰ、Ⅱ
     C. 仅Ⅲ
     D. 仅Ⅰ、Ⅲ

102. 1.（全国联考2010年）对下图进行拓扑排序，可以得到不同的拓扑序列的个数是________。
     A. 4
     B. 3
     C. 2
     D. 1

103. 1.（全国联考2010年）若无向图G = <V, E>中含有7个顶点，要保证图G在任何情况下都是连通的，则所需的边数最少是________。
     A.6
     B.15
     C.16
     D.21

> 任何情况下都连通，表示无论边怎么替换，图依旧保持连通；n个顶点任何情况下连通的最少边数：n-1个顶点构成完全连通分图，并有一个边连接剩下的结点    

104. 1.（全国联考2017下）下列关于最小生成树的叙述
     Ⅰ. 最小生成树的代价唯一
     Ⅱ. 所有权值最小的边一定会出现在所有的最小生成树中
     Ⅲ. 使用普里姆（Prim）算法从不同顶点开始得到的最小生成树一定相同
     Ⅳ. 使用普里姆算法和克鲁斯卡尔（Kruskal）算法得到的最小生成树总不相同
     其中，正确的是________。
     A. 仅Ⅰ
     B. 仅Ⅱ
     C. 仅Ⅰ、Ⅲ
     D. 仅Ⅱ、Ⅳ

105. 1.（全国联考2012年）对如下有向带权图，若采用迪杰斯特拉（Dijkstra）算法求从源点a到其他各顶点的最短路径，则得到的第一条最短路径的目标顶点是b，第二条最短路径的目标顶点是c，后续得到的其余各最短路径的目标顶点依次是________。
     A.d,e,f
     B.e,d,f
     C.f,d,e
     D.f,e,d

106. 1.（全国联考2012年）若用邻接矩阵存储有向图，矩阵中主对角线以下的元素均为零，则关于该图拓扑序列的结论是________。
     A. 存在，且唯一
     B. 存在，且不唯一
     C. 存在，但可能不唯一
     D. 无法确定是否存在

107. 1<u>.（全国联考2012年）对有n个顶点、e条边且使用邻接表存储的有向图进行广度优先遍历，其算法时间复杂度是________。</u>
     A. O(n)
     B. O(e)
     C. O(n+e)
     D. O(n×e)

108. 1.（河北大学2019年）如果要求必须进行两次广度优先搜索才能访问所有顶点，那么下列说法中不正确的是________。
     A.G肯定不是完全图
     B.G中一定有回路
     C.G一定不是连通图
     D.G有2个连通分量

109. 36.浙江理工大学2019年]带权有向图G用邻接矩阵A存储，则顶点的入度等于A中(
      A.第行非零元素之和
      B.第列非零元素之和
      C.第行非零且非无穷元素个数
      D.第列非零且非无穷元素个数

110. 1.（深圳技术大学2019年）n个顶点e条边的连通图，生成树有________条边。
     A. n-1
     B. n
     C. n+1
     D. e

111. 1.（中国计量大学2016年）关于下图所示的邻接矩阵，若节点A的出度为2，则描述错误的是________。
     A. 节点A到节点C的最短路径长度为4
     B. 节点B的度为3
     C. 节点A到C只有1条路径
     D. 节点F的度为2

112. 39.[中国科学院大学2015年]无向图G=(V,E),其中V={a,b,c,d,e,f,E={(a,b),(a,e,(a,c),(b,e),(c,f),(f,d),(e,d)},对该图进行深度优先遍历，得到的顶点序列正确的是(
     为
     A.a,b,e,c,d,f
     B.a,c,f,e,b,d
     C.a,e,b,c,f,d
     D.a,e,d,f,c,b

113. 40.[河北师范大学2020年]已知无向图G=(V,E),其中V={a,b,c,d,e,f},E={(a,b),(a,e),(a,c),(b,e),(c,f),(f,d),(e,d}。对该图进行深度优先遍历，不能得到的序列是(
     )o
     A.acfdeb
     B.aebdfc
     C.aedfcb
     D.abecdf

114. 41.[贵州师范大学2017年]若G是一个共有n个顶点的有向完全图，则图中共有________条弧。
     A. n*(n-1)/2
     B. n*(n-1)*2
     C. n*(n-1)
     D. n*(n+1)/2

115. 46.[广东工业大学2017年]深度为6的二叉树最多有________个节点。
     A. 32
     B. 31
     C. 64
     D. 63

116. 47.[中国科学技术大学2015]n(n>0)个分支节点的满二叉树的深度是________。
     A. n^2-1
     B. log₂(n+1)+1
     C. log₂(n+1)
     D. log₂(n-1)

117. 48.[武汉科技大学2016年]二叉树先序遍历y在x之前，后序遍历x在y之后，则x是y的________（题目不完整）
     A. 左兄弟
     B. 右兄弟
     C. 祖先
     D. 后裔

118. 49.广东工业大学2017年已知某二叉树的中序遍历序列是D.E.A.B.C，前序遍历序列是B.A.E.D.C，则其后序遍历序列是________。
     A.D,E,A,C,B
     B.D,E,B,A,C
     C.B,A,E,C,D
     D.B,D,A,E,C

119. 50.[西南师范大学2014年]在有n个叶子节点的哈夫曼树中，其节点总数为________。
     A. 不确定
     B. 2n
     C. 2n+1
     D. 2n-1

120. 52.[中国科学院大学2015年]一棵二叉树的前序遍历序列为A.B.C.D.E.F.G，它的中序遍历序列可能是________。
     A. C.A.B.D.E.F.G
     B. A.B.C.D.E.F.G
     C. D.A.C.E.B.F.G
     D. A.D.F.C.E.G.B

121. 53.[宁波大学2019年]下列二叉树中，(________)可用于实现不等长的编码。
     A.最优二叉树
     B.B-树
     C.平衡二叉树
     D.二叉排序树

122. 1.[桂林电子科技大学2015年]队列的特点是(________)。
     A.允许在表的任何位置进行插入和删除
     B.只允许在表的一端进行插入和删除
     C.允许在表的两端进行插入和删除
     D.只允许在表的一端进行插入，在另一端进行删除

123. 2.南京理工大学2013年用一个大小为6的数组来实现循环队列，且当前rear和front的值分别为0和3。当从队列中删除一个元素，再加入两个元素后，rear和front的值分别为多少？(________)
     A.1和5
     B.2和4
     C.4和2
     D.5和1

124. 3.[全国联考2017年]适用于压缩存储稀疏矩阵的两种存储结构是(________)。
     A.三元组表和十字链表
     B.三元组表和邻接矩阵
     C.十字链表和二叉链表
     D.邻接矩阵和十字链表

125. 4.全国联考2016年有一个100阶的上三角矩阵M，其元素m[i][j] (1≤i≤100，1≤j≤100)按行优先顺序压缩存入下标从0开始的一维数组N中。元素m[30][30]在N中的下标是(________)。
     A.86
     B.87
     C.88
     D.89

126. 5.[中国科学院大学2015年]若6行5列的数组以行序为主序顺序存储，基地址为1000，每个元素占2个存储单元，则第3行第4列的元素(假定无第0行第0列)的地址是(________)。
     A.1040
     B.1042
     C.1026
     D.（无选项）

127. 6.广东工业大学2017年下列关于数组的描述，正确的是(________)。
     A.数组的大小是固定的，但可以有不同的类型的数组元素
     B.数组的大小是可变的，所有数组元素的类型必须相同
     C.数组的大小是固定的，所有数组元素的类型必须相同
     D.数组的大小是可变的，可以有不同的类型的数组元素

128. <mark>7.[常州大学2015年]广义表L=((),(e),(a,(b,c,d))的表尾和深度为</mark>
     A.tail(L)=((e),(a,(b,c,d))),depth(L)=2
     B.tail(L)=(a,(b,c,d)),depth(L)=2
     C.tail(L)=((e),(a,(b,c,d))),depth(L)=3
     D.tail(L)=(a,(b,c,d)),depth(L)=3

> 表尾指去掉表头的子表，深度指递归深度，表头的深度为1，表头是第一个元素代表的子表

129. 8.电子科技大学2015年广义表((a,(b,c)),d,e)的表头是(________)。
     A. a
     B. (a),((b),c)
     C. (a)
     D. ((b),c)

130. 已知无向图G如下所示，使用克鲁斯卡尔(Kruskal)算法实现G的最小生成树，加到最小生成树中的边依次是(________)
     A.(b,f)(b,d)(a,e)(c,e)(b,e)
     B.(b,f)(b,d)(b,e)(a,e)(c,e)
     C.(a,e)(b,e)(c,e)(b,d)(b,f)
     D.(a,e)(c,e)(b,e)(b,f)(b,d)

131. <u>修改递归方式实现的图的深度优先搜索(DFS)算法，将输出（访问）顶点信息的语句移动到退出递归前（即执行输出语句后立即退出递归）。采用修改后的算法遍历有向无环图G，若输出结果中包含G中的全部顶点，则输出的顶点序列是G的</u>
     A.拓扑有序序列
     B.逆拓扑有序序列
     C.广度优先搜索序列
     D.深度优先搜索序列
     
     > 原本的递归dfs顺序：1.若空return 否则输出结点值 2.遍历子节点，若没经过则递归子节点 3.遍历完成，return

132. 使用Dijkstra算法求下图中从顶点1到其余各点的最短路径，将当前找到的从顶点1到顶点2、3、4、5的最短路径长度保存在数组dist中，求出第二条最短路径后，dist中的内容更新为
     A.26,3,14,6
     B.21,3,14,6
     C.25,3,14,6
     D.15,3,14,6

> 本题答案有误，应为B

133. 给定如下有向图，该图的拓扑有序序列的个数是(
     A.1
     B.2
     C.3
     D.4

134. 下图是一个有10个活动的AOE网，时间余量最大的活动是(
     A.c
     B.g
     C.h
     D.j

> 本题答案有误，应为B 

135. 对于无向图G = (V, E)，下列选项中，正确的是(
     A.当|V| > |E|时，G一定是连通的
     B.当|V| < |E|时，G一定是连通的
     C.当|V| = |E| - 1时，G一定是不连通的
     D.当|V| > |E| + 1时，G一定是不连通的

> 特定情况下最少可能连通边数(线性)E1:n-1 任意情况下最小必然连通边数E2:(n-2+1)(n-2)/2+1 (n-1个结点构成完全图，有一边连通完全图和孤立点) 当E>=E2时一定连通，E<E1时不可能连通

136. 已知无向连通图G中各边的权值为1，下列算法中，一定能够求出图G中从某顶点到其余各顶点最短路径的是(
     Ⅰ.普里姆(Prim)算法 Ⅱ.克鲁斯卡尔(Kruskal)算法
     Ⅲ.图的广度优先搜索算法
     A.仅Ⅰ
     B.仅Ⅲ
     C.仅Ⅰ，Ⅱ
     D.Ⅰ，Ⅱ，Ⅲ

137. 28.(全国联考2011年)已知一棵有2011个节点的树，其中节点个数为116，该树对应的二叉树中无右孩子的节点个数是(
     A.115
     B.116
     C.1895
     D.1896

138. 29.(全国联考2010年)<u>下列线索二叉树中（用虚线表示线索），符合后序线索树定义的是</u>(
     （注：题目含图形选项A、B、C、D）

> 线索树定义：若父节点左子节点为空，则该指针指向前序结点；若右指针为空则指向后继结点

139. 30.(全国联考2010年)在一棵度为4的树T中，若有20个度为4的节点，10个度为3的节点，1个度为2的节点，10个度为1的节点，则树T的叶节点个数是(
     A.41
     B.82
     C.113
     D.122

140. 31.(全国联考2010年)对n ≥ 2个权值互不相同的字符构成哈夫曼树，下列关于该哈夫曼树的叙述中，错误的是(
     A.该树一定是一棵完全二叉树
     B.树中一定没有度为1的节点
     C.树中两个权值最小的节点一定是兄弟节点
     D.树中任一非叶节点的权值一定不小于其下一层任一节点的权值

141. 32.(全国联考2009年)给定二叉树如下图所示。设N代表二叉树的根，L代表节点的左子树，R代表节点的右子树。若遍历后的节点序列是3,1,7,5,6,2,4，则其遍历方式是________
     A. LRN
     B. NRL
     C. RLN
     D. RNL

> 本题没有图

142. 33.(全国联考2009年)已知一棵完全二叉树的第6层（设根为第1层）有8个叶节点，则该完全二叉树的节点个数最多是________。
     A. 39
     B. 52
     C. 111
     D. 119

> 两种情况：第六层为倒数第二层或最后一层

143. 34.(全国联考2009年)将森林转换为对应的二叉树，若在二叉树中，节点u是节点v的父节点的父节点，则在原来的森林中，u和v可能具有的关系________。
     Ⅰ.父子关系
     Ⅱ.兄弟关系
     Ⅲ.u的父节点与v的父节点是兄弟关系
     A.只有Ⅱ
     B.Ⅰ和Ⅱ
     C.Ⅰ和Ⅲ
     D.Ⅰ、Ⅱ和Ⅲ

> 树转二叉树的方法：1.连接所有兄弟节点 2.砍掉除了左子结点外的其他连接 3.顺时针转动 

144. 35.(广东财经大学2022年)按照满二叉树的编号顺序对深度为k的完全二叉树编号，则编号最小的叶节点的编号是________。
     A. \(2^{k-1}-1\)
     B. \(2^{k-1}\)
     C. \(2^{k-2}+1\)
     D. \(2^k-1\)

145. 36.(暨南大学2022年)<u>在含有N个节点的线索二叉树中线索的数目为</u>________。
     A. 2N
     B. N
     C. N-1
     D. N+1

> N个结点的线索二叉树中有N+1个空结点

146. 25.(浙江理工大学2019年)设计一个判别表达式中括号是否匹配的算法，采用________数据结构最佳。
      A. 顺序
      B. 链表
      C. 队列
      D. 栈

147. 26.(烟台大学2019年)设有数组A[i,j]，数组的每个元素长度为3字节，i的值为1到8，j的值为1到10，数组从内存地址adr开始顺序存放，当以列为主存放时，元素A[5,8]的存储地址为________。
     A. adr+141
     B. adr+180
     C. adr+222
     D. adr+225

148. 27.(中国计量大学2019年)<u>在软件的操作过程中，一般都有撤销或返回的操作，如果要实现该功能，必须保存历史操作的信息，应该用什么数据结构来保存这些历史操作信息</u>？________
     A. 队列
     B. 查找二叉树
     C. 有向图
     D. 栈

> 先撤回最后的版本，再撤回之前的版本，后进先出，所以栈最适合

149. 28.(青岛科技大学2017年)链表和顺序栈相比，一个比较明显的优势是________。
     A. 插入操作更加方便
     B. 删除操作更加方便
     C. 不会出现栈空的情况
     D. 通常不会出现栈满的情况

150. 29.(中国科学院大学2015年)<u>若一个栈以向量V[1..n]存储，初始栈顶指针top为n+1，则下面x入栈的正确操作是________。</u>
     A. top=top-1; V[top]=x
     B. V[top]=x;top=top+1
     C. top=top+1;V[top]=x
     D. V[top]=x;top=top-1

> 初始栈顶指针为n+1，说明以n为栈底向下存储，且初始指针在向量外，先-后装

---

## 答案区

101-105：CBCAC
106-110：CCBDA
111-115：CDDCD
116-120：BCADB
121-125：ADBAB
126-130：CCCBA
131-135：BCADD
136-140：BDDBA
141-145：DCBCD
146-150：DBDDC
