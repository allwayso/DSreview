# 选择题自由练习 151-200

---

## 题目区

151. 30.【桂林电子科技大学2015年】若出栈的顺序是a, b, c, d, e，则入栈的顺序不可能是( )。
     A. a, b, c, d, e
     B. e, d, c, b, a
     C. d, e, c, b, a
     D. a, e, d, c, b

152. 31.【桂林电子科技大学2015年】常用于函数调用的数握结构是( )。
     A. 栈
     B. 队列
     C. 链表
     D. 数组

153. 32.【暨南大学2022年】若有一个栈的输入序列是1, 2, 3, ..., n，输出序列的第一个元素是n，则第i个输出元素是( )。
     A. n-i
     B. n-i-1
     C. n-i+1
     D. 不确定

154. 10.【全国联考2019年】<u>若将一棵树T转化为对应的二叉树BT时，则下列对BT的遍历中，其遍历序列与T的后根遍历序列相同的是</u>( )。
     A. 先序遍历
     B. 中序遍历
     C. 后序遍历
     D. 按层遍历

155. 11.<u>对n个互不相同的符号进行哈夫曼编码，若生成的哈夫曼树共有115个节点，则n的值是</u>( )。
     A. 56
     B. 57
     C. 58
     D. 60

> n个字符的哈夫曼树结点数为2n-1

156. 12.设一棵非空完全二叉树T的所有叶节点均位于同一层，且每个非叶节点都有2个子节点。若T有k个叶节点，则T的节点总数是( )。
     A. 2k-1
     B. 2k
     C. k*2
     D. 2^k-1

157. 13.已知字符集{a, b, c, d, e, f}，若各字符出现的次数分别为6, 3, 8, 2, 10, 4，则对应字符串中的各字符的哈夫曼编码可能是( )。
     A. 00, 1011, 01, 1010, 1, 100
     B. 00, 100, 110, 000, 0010, 01
     C. 10, 1011, 11, 001, 00, 010
     D. 0011, 10, 1, 0010, 01, 000

> 1.看频率是否和编码长度大致成反比 2.看看有没有某字符编码为另一字符的前缀 3.由选项建树是否合理

158. 14.要使一棵非空二叉树的先序序列与中序序列相同，其所有非叶节点须满足的条件是( )。
     A. 只有左子树
     B. 只有右子树
     C. 节点的度均为1
     D. 节点的度均为2

159. 15.已知一棵二叉树的树形如下图所示，其后序序列为e,a,c,b,d,g,f，树中与节点a同层的节点是( )。
     A. c
     B. d
     C. f
     D. g

160. 16.已知字符集{a,b,c,d,e,f,g,h}，若各字符的哈夫曼编码依次是0100,10,0000,0101,001,11,0001,00，则编码序列0100110001011100101的译码结果是( )。
     A. a,c,g,b,a,h,f
     B. a,d,a,g,b,b,b
     C. a,f,b,e,a,g,d
     D. a,e,f,c,a,g,d

161. 17.若森林F有15条边、25个节点，则F包含树的个数是( )。
     A. 8
     B. 9
     C. 10
     D. 11

162. <u>18.先序序列为a,b,c,d的不同二叉树的个数是</u>( )。
     A. 13
     B. 14
     C. 15
     D. 16

> n个结点构成不同二叉树满足卡特兰公式： $C_n = \frac{1}{n+1}\binom{2n}{n} = \frac{(2n)!}{(n+1)! \cdot n!}$    

163. 19.【全国联考2018年】设有一个12×12的对称矩阵M，将其上三角部分的元素m[i][j](1 ≤ i ≤ j ≤ 12)按行优先存入C语言的一维数组N中，元素m[6][6]在N中的下标是( )。
      A. 30
      B. 51
      C. 55
      D. 66

164. 10.【全国联考2017年】下列关于栈的叙述中，错误的是( )。
     Ⅰ.采用非递归方式重写递归程序时必须使用栈
     Ⅱ.函数调用时，系统要用栈保存必要的信息
     Ⅲ.只要确定了入栈顺序，就可确定出栈顺序
     Ⅳ.栈是一种受限的线性表，只允许在其两端进行操作
     A. 仅Ⅰ
     B. 仅Ⅰ、Ⅱ、Ⅲ
     C. 仅Ⅰ、Ⅲ、Ⅳ
     D. 仅Ⅱ、Ⅲ、Ⅳ

165. 11.【全国联考2015年】已知程序如下：
     int S(int n) {
     return (n==0)? 0 : S(n-1)+n;
     }
     void main() {
     cout<<S(1);
     }
     程序运行时使用栈来保存调用过程的信息，自栈底到栈顶保存的信息依次对应的是( )。
     A. main()-S(1)-S(0)
     B. S(0)-S(1)-main()
     C. main()-S(0)-S(1)
     D. S(1)-S(0)-main()

166. <u>12.【全国联考2014年】假设栈初始为空，将中缀表达式a+b*c-(d+e*f)/g转换为等价的后缀表达式的过程中，当扫描到f时，栈中的元素依次是( )。</u>
     A. +( -*
     B. +(-*
     C. +( *-
     D. /+-

> 中缀转后缀的逻辑：1.遇到数字：直接写入结果string 2.遇到（：直接入运算符栈 3.遇到）：弹出所有非（运算符加在结果string后，再弹出（ 4.其他运算符：弹出运算符栈顶元素直到栈顶元素的优先级比当前运算符严格低或栈空，再把当前运算符入栈 5.处理完所有输入字符后，将运算符栈的剩余元素加到结果串末尾

167. <u>13.【全国联考2013年】一个栈的入栈序列为1,2,3,…,n，其出栈序列是p₁,p₂,p₃,…,pₙ。若p₂=3，则p₃可能取值的个数是( )</u>。
     A. n-3
     B. n-2
     C. n-1
     D. 无法确定

168. 14.【全国联考2012年】已知操作符包括“+、-、*、/、(、)”。将中缀表达式a+b-a*((c+d)/e-f)+g转换为等价的后缀表达式ab+acd+e/f-*-g+时，用栈来存放暂时还不能确定运算次序的操作符。若栈初始为空，在转换过程中，栈中同时保存的操作符的最大个数为( )。
     A. 5
     B. 7
     C. 8
     D. 11

169. 15.【全国联考】元素a,b,c,d,e依次进入初始为空的栈中，若元素进栈后可停留，可出栈，直到所有元素都出栈，则在所有可能的出栈序列中，以元素d开头的序列的个数是( )。
     A. 3
     B. 4
     C. 5
     D. 6

170. 16.【全国联考2010年】元素a,b,c,d,e,f依次进栈，允许进栈、退栈操作交替进行，但不允许连续三次进行退栈操作，则不可能得到的出栈序列是( )。
     A. d,c,b,a,e,f
     B. c,b,d,a,e,f
     C. b,c,a,e,f,d
     D. a,f,e,d,c,b

171. 19.下列选项给出的是从根分别到达两个叶节点路径上的权值序列，能属于同一棵哈夫曼树的是( )
     A. 24,10,5和24,10,7
     B. 24,10,5和24,12,7
     C. 24,10,10和24,14,11
     D. 24,10,5和24,14,6

172. ~~本题无图20.若对如下图所示的二叉树进行中序线索化，则节点x的左、右线索指向的节点分别是~~
     A. e, c
     B. e, a
     C. d, c
     D. b, a

173. <u>21.将森林F转换为对应的二叉树T，F中叶节点的个数等于( )</u>。(全国联考2014年)
     A. T中叶节点的个数
     B. T中度为1的节点个数
     C. T中左孩子指针为空的节点个数
     D. T中右孩子指针为空的节点个数

174. 22.5个字符有如下4种编码方案，不是前缀编码的是( )。(全国联考2014年)
     A. 0,100,000,001,001,1
     B. 01,100,001,011,010,1
     C. 00,000,010,011,100
     D. 0,100,110,1110,1100

175. <u>23.已知二叉树T中6个叶节点的权分别是2,3,4,5,6,7，T的带权（外部）路径长度最小是( )（本题构建出来的哈夫曼三叉树）</u>
     A. 27
     B. 46
     C. 54
     D. 56

> 三叉树的合并：1.判断当前结点数n是否满足(n-1)%2==0 若否则添加0结点 2.合并同二叉树

176. 24.若X是后序线索二叉树的叶节点，且X存在左兄弟节点Y，则X的右线索指向的是( )。
     A. X的父节点
     B. 以Y为根的子树的最左下节点
     C. X的左兄弟节点Y
     D. 以Y为根的子树的最右下节点

177. 25.若一棵二叉树的前序遍历序列为a, e, b, d, c，后序遍历序列为b, c, d, e, a，则根节点的孩子节点( )。
     A. 只有e
     B. 有e, b
     C. 有e, c
     D. 无法确定

178. 26.若一棵完全二叉树有768个节点，则该二叉树中叶节点的个数是( )。
     A. 257
     B. 258
     C. 384
     D. 385

179. 27.若一棵二叉树的前序遍历序列和后序遍历序列分别为1,2,3,4和4,3,2,1，则该二叉树的中序遍历序列不会是( )。
     A. 1,2,3,4
     B. 2,3,4,1
     C. 3,2,4,1
     D. 4,3,2,1

180. 17.某队列允许在其两端进行入队操作，但仅允许在一端进行出队操作。若元素a, b, c, d依次从队后再进行出队操作，则不可能得到的出队序列是( )。(全国联
     A. b, a, c, d, e
     B. d, b, a, c, e
     C. d, b, c, a, e
     D. e, c, b, a, d

181. 18.为解决计算机主机与打印机之间速度不匹配问题，通常设置一个打印数据缓冲区，主机将要输出的数据依次写入该缓冲区，而打印机则依次从该缓冲区中取出数据。该缓冲区的逻辑结构应该是( )。(全国联考2009年)
     A. 栈
     B. 树
     C. 队列
     D. 图

182. <u>19.设栈S和队列Q的初始状态均为空，元素a,b,c,d,e,f,g依次进入栈S。若每个元素出栈后立即进入队列Q，且7个元素出队的顺序是b,d,c,f,e,a,g，则栈S的容量至少是( )。(全国联考2009年)</u>
     A. 1
     B. 2
     C. 3
     D. 4

> 队列的入队顺序等于出队顺序

183. 20.在容量为n的循环队列中，f指向第一个元素，r指向队尾插入位置，则队列中元素个数为( )。(北京邮电大学2022年)
     A. r-f+1
     B. r-f
     C. (r-f)mod n+1
     D. (r-f)mod n

184. 21.若栈存放在数组stack[MAXSIZE]中，栈顶位置top的值为-1，则元素e进栈的操作是( )。(广东财经大学2022年)
     A. stack[top++] = e;
     B. stack[++top] = e;
     C. stack[top--] = e;
     D. stack[top] = e;

185. 22.设数组data[m]作为循环队列Q的存储空间，front为队头指针，rear为队尾指针，则执行出队操作后，队头指针front为( )。(河北建筑工程学院2021年)
     A. front=front-1
     B. front=(front+1)%(m-1)
     C. front=(front+1)%m
     D. front=front+1%m

186. 23.栈和队列都属( )。(河北建筑工程学院2020年)
     A. 顺序存储的线性结构
     B. 链式存储的非线性结构
     C. 限制存取点的线性结构
     D. 限制存取点的非线性结构

187. 24.已知一个栈的顺序栈序列为a1,a2,…,an，输出为1,2,…,n，若a3=1，则a1( )。(河北师范大学2020年)
     A. 可能是2
     B. 一定是2
     C. 不可能是2
     D. 不可能是3

188. 1.【电子科技大学2014】一个栈的输入序列为1,2,3,4,5，则下列序列中不可能是栈的输出序列的是( )。
     A. 2,3,4,1,5
     B. 5,4,1,3,2
     C. 2,3,1,4,5
     D. 1,5,4,3,2

189. 2.【广东工业大学2017】假设栈的入栈序列为1,2,3,…,n，出栈序列为p1,p2,p3,…,pn，若p2=2，则p3可能取值的个数是( )。
     A. n-1
     B. n-2
     C. n-3
     D. 无法确定

190. ~~3.设有如下图所示的火车车站，入口到出口之间有n条轨道，列车的行进方向均为从左至右，列车可驶入任意一条轨道。现有编号为1-9的序列列车，驶入的次序依次为8,4,2,5,3,9,1,6,7。若期望驶出的次序改为1-9，则至少需要( )。~~本题无图
     A. 2
     B. 3
     C. 4
     D. 5

191. 4.【全国联考2014年】假设循环队列的数组A[M:1..M]中，end1指向队头元素，end2指向队尾元素的下一个位置。初始时队列空间闲置，从队列中最多能取出M-1个元素。初始时空，下列判断队空和队满的条件中，正确的是( )。
     A. 队空: end1==end2；队满: end2==(end1+1)mod M
     B. 队空: end1==end2；队满: end1==(end2+1)mod M+1
     C. 队空: end2==(end1+1)mod M；队满: end1==(end2+1)mod M
     D. 队空: end1==(end2+1)mod M；队满: end2==(end1+1)mod(M-1)

192. 5.【全国联考2013年】已知循环队列存储在一维数组A[0..n-1]中，且队列非空时，front和Rear分别指向队头元素和队尾元素。若初始时队列为空，且要求第1个进入队列的元素存储在A[0]处，则此时front和Rear的分别是( )。
     A. 0,0
     B. 0,n-1
     C. n-1,0
     D. n-1,n-1

193. 6.【中国计量大学2017年】往队列中输入序列1,2,3,4，然后出队1个元素，则出队的数字是( )。
     A. 4
     B. 3
     C. 1
     D. 不确定

194. 7.【广东工业大学2017年】下列操作中，不属于双端队列的操作是( )。
     A. 取队头元素
     B. 取队尾元素
     C. 取队元元素
     D. 插入队尾元素

195. 8.【电子科技大学2014年】最大容量为n的循环队列，队尾指针是rear，队头指针是front，则队满的条件是( )。
     A. (rear+1)mod n == front
     B. rear == front
     C. rear+1 == front
     D. (rear+1)mod n != front

196. 9.【河南理工大学2021年】二叉树的第k层上最多含有节点数为( )。
     A. 2*k
     B. 2^(k-1)-1
     C. 2^(k-1)
     D. 2*k-1

197. 10.【河北师范大学2020年】若一棵深度为6的完全二叉树的第6层有3个叶子节点，则该二叉树中节点数目是( )。
     A. 17
     B. 18
     C. 19
     D. 20

198. 11.【湖北大学2020年】若一棵二叉树有15个度为2的节点，10个度为1的节点，则度为0的节点个数是( )。
     A. 16
     B. 25
     C. 40
     D. 不确定

199. 12.【湖南大学2020年】根据使用频率，为5个字符设计的哈夫曼编码不可能是( )。
     A. 000,001,010,011,1
     B. 0000,0001,001,01,1
     C. 000,001,01,10,11
     D. 0000,0001,001,010,011

200. 13.【广东财经大学2021年】若按照从上到下、由左至右的顺序依次深度，深度为6的完全二叉树编号最大的节点编号是( )。
     A. 63
     B. 64
     C. 126
     D. 127

---

## 答案区

151-155：CACBC
156-160：AABBD

161-165：CBACA
166-170：BCABD

171-175：DACDB
176-180：AACCC

181-185：BCDBD
186-190：DCBAC

191-195：ABCAA
196-200：CAACD
